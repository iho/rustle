[package]
name = "unchecked_promise_result"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "unchecked_promise_result"
path = "src/main.rs"

[dependencies]
near_core = { path = "../near_core" }
llvm-sys = "211"
