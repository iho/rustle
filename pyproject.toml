[project]
name = "rustle"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pytablewriter",
    "toml",
    "tqdm",
]

[tool.black]
line-length = 120
target-version = ['py313']

[tool.ruff]
ignore = ["E501"]
line-length = 120
